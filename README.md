# home-assistant-lan-scanner
# LAN Scanner — Home Assistant Integration  Custom integration for Home Assistant that scans your local network, discovers devices (IP, MAC, hostname, vendor), detects WiFi cameras with only port 554 open (RTSP), and lets you assign persistent friendly names by MAC address.
